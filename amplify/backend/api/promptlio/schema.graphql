type Userdata @model @auth(rules: [{allow: owner}]) {
  id: ID!
  test_text: String
}

type Todo @model @auth(rules: [{allow: owner operations: [create, read, update, delete]}, {allow: private, operations: [create, read]}]) {
  id: ID!
  name: String!
  description: String
}
 